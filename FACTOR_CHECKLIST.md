# 新因子准入检查清单（Factor Admission Checklist）

> 版本: v1.0 (2026-07-17)
> 来源: 新闻因子 P1-P5 三次证伪案例的教训沉淀（原研究 +20%、P3 +2.94% 前视、P2 -0.036 过拟合）
> 配套工具: `utils/factor_checks.py`（五道关卡中机器可判的三道已函数化）
> 适用范围: 所有候选进入主体系（整合体系：多资产 + 中证1000增强 + 择时）的新因子/信号

---

## 总原则

1. **因子有举证责任**：新因子必须在时间稳定性和样本外上证明自己，而不是我们证明它无效。证据不足 = 不通过。
2. **统计显著 ≠ 可交易**：t 值再大，收益起算点错了就是假的（P3 t=12 照样证伪）。
3. **4.5 个月单 regime 的显著性一文不值**：单一牛市/熊市里"发现"的信号，默认是 regime 拟合。
4. **宁可错杀，不可错放**：错放一个假因子的成本（污染组合、占用研究时间）远高于错杀一个真因子。

---

## 五道关卡（全部通过才可进组合）

### 关卡0: 收益起算点审查（前视检查）🔴 一票否决

**先于一切统计检验执行。**

- [ ] 信号在 T 时刻的取值，是否只用了 T 时刻**及之前**可得的信息？
- [ ] "观察到某形态后再入场"类信号（如"跌不动才买"、"确认突破才买"）：收益是否从**观察完成点**起算？
  - 反例（P3）：用 T+1~T+3 观察"不跌"，却把 T~T+3 的收益算进信号收益 → 87% 是选择窗口的机械收益
  - 正确做法：`r_entry_to_exit = (1+ret_exit)/(1+ret_entry)-1`，入场点=形态确认日收盘
- [ ] 新闻/公告类：发布时间 vs 建仓时点是否严格对齐？（盘前新闻当日可交易，盘后新闻 T+1 才可交易）
- [ ] 财务/宏观数据：是否使用了**发布日**而非报告期？（PIT 对齐）
- [ ] 分组条件本身是否含未来信息？（幸存者、事后分箱、用全样本归一化参数）

**任何一个"不确定"→ 先解决，再继续。起算点错了，后面所有检验都是废纸。**

### 关卡1: 样本内显著性（必要不充分）

- [ ] 收益序列先 **winsorize（1%/99%）** 再算 IC 和分层（极端值污染见 P2 五分位 67.8% 假象）
- [ ] 日度截面 Spearman IC：|t| ≥ 2（注意 rank 后 Pearson = Spearman，环境无 scipy 时用 `utils/factor_checks.ic_daily`）
- [ ] 五分位/十分位**单调性**：Q1→Q5 应大致单调；只有两端有效、中间混乱 = 弱证据
- [ ] 多空收益（Q1-Q5）方向与 IC 符号一致
- [ ] 报告 IC 均值 **和** IC 序列（不是只看汇总值）

### 关卡2: 时间稳定性 🔴（最便宜的过拟合检查）

- [ ] **分半检验**：按日期中位数分前后两半，两半 IC **同号**且各 p < 0.1
  - P2 案例：前半 p=0.36 / 后半 p=0.007 → 信号只活在一半时间里 → 判过拟合
- [ ] 滚动 IC（20 日窗口）：观察是否只在特定 regime 显著
- [ ] 若样本跨越不同市场环境（牛/熊/震荡），分 regime 各算一次 IC

### 关卡3: 样本外验证 🔴（决定生死）

- [ ] 样本外数据必须是**不同时间段**，且最好含**不同 regime**
- [ ] 通过标准：样本外 IC 与样本内**同号**且 |t| ≥ 1.65
- [ ] 打分器/构造方式必须与样本内**完全一致**（换打分器 = 换了因子，结论只能算旁证）
- [ ] 数据可行性声明：若无真样本外数据，必须显式写明"未验证 = 不通过"，不得含糊
- [ ] 功效声明：样本外样本量/天数不足时，只能说"无法检验"，不能说"验证通过"

### 关卡4: 可交易性

- [ ] 扣费后仍有效：双边成本 + 滑点（A股按 ~0.2-0.3% 双边估）
- [ ] 涨跌停/T+1 约束下信号可执行
- [ ] 容量：信号涉及的股票流动性是否撑得起目标仓位
- [ ] 与组合现有因子相关性：|ρ| > 0.6 的增量价值存疑
- [ ] 信号覆盖率：太稀疏（如 DOS 极端预警）做不了日常因子，只能做风控开关

---

## 红旗清单（任一命中 → 默认拒绝）

| # | 红旗 | 案例 |
|---|------|------|
| 1 | 收益起算点早于信号确认点 | P3：87% 收益在选择窗口 |
| 2 | 分半 IC 一半不显著 | P2：前半 p=0.36 |
| 3 | 只在单一 regime 显著 | 原研究 +20%（单一牛市） |
| 4 | 对照组也上涨，信号组仅略高 | P3 残差：大跌组 T+10 也 +0.69% |
| 5 | 样本外方向相反或归零 | P2 样本外 IC +0.077（反号）；P3 残差 +0.03% |
| 6 | 极端值驱动（winsorize 后消失） | P2 五分位 67.8% 假象 |
| 7 | 小样本分组（<50）却给结论 | P5 事件×情感组合 |
| 8 | 模型改善全靠价格特征，目标特征零贡献 | GBDT：DOS/news 零入树 |
| 9 | 事后挑样本定义信号组 | "事后挑出不跌的"= 前视 |
| 10 | 多重比较不调参（试了 N 个因子报 1 个显著的） | 研究惯例 |

---

## 数学/方法陷阱清单

1. **常数平移秩不变**：日内减去市场均值（rel = x - 当日均值）不改变当日截面排序 → 与绝对值 IC 完全相同。相对因子只有**组内相对**（行业/风格）才有新信息。（P2 验证中两列 IC 逐位相同）
2. **选择窗口收益**：信号定义用到的观察窗口，其收益在实盘中拿不到，必须从收益计算中剔除。
3. **leave-one-out**：构造行业/市场均值时包含自己，会引入机械相关。
4. **分层均值被极端值绑架**：先 winsorize 再分层。
5. **胜率陷阱**：胜率 60% 不等于期望收益为正（看均值和 t 值）。

---

## 快速使用

```python
from utils.factor_checks import run_factor_admission
import pandas as pd

df = pd.read_csv("your_factor_panel.csv")  # 需含 date, 因子列, 远期收益列
verdict = run_factor_admission(
    df, factor="your_factor", ret="ret_2d", date="date",
    oos_df=None,          # 有样本外面板就传入；没有 = 关卡3 不通过
)
# 返回 {'gate1_in_sample': ..., 'gate2_split_half': ..., 'gate3_oos': ...}
# 关卡0(前视)与关卡4(可交易性)按本清单人工确认
```

事件驱动类信号（观察形态后入场）必做：

```python
from utils.factor_checks import post_entry_return, welch_t
df["r_post"] = post_entry_return(df, entry_col="ret_3d", exit_col="ret_5d")
t, p = welch_t(df.loc[df["signal"], "r_post"], df.loc[df["control"], "r_post"])
```

---

## 检查记录模板（每个候选因子留档）

| 项目 | 内容 |
|------|------|
| 因子名称 / 日期 / 作者 | |
| 信号定义与入场时点 | （关卡0：逐条回答前视问题） |
| 样本区间 / regime | |
| 关卡1 结果 | IC=, t=, 分层单调性= |
| 关卡2 结果 | 前半 IC/t/p = ；后半 IC/t/p = |
| 关卡3 结果 | 样本外区间=，IC/t/p=，打分器一致性= |
| 关卡4 结果 | 扣费后=，容量=，与现有因子ρ= |
| **最终判定** | ☐ 准入 ☐ 拒绝（原因： ） |

---

## 案例回溯：因子在本清单下的判定

| 因子 | 关卡0 | 关卡1 | 关卡2 | 关卡3 | 判定 |
|------|-------|-------|-------|-------|------|
| P2 板块相对情感 | ✅ 无起算点问题 | ✅ t=-2.98 | ❌ 前半 p=0.36 | ❌ 样本外反号/零支持 | **拒绝（过拟合）** |
| P3 负面超跌反转 | ❌ 87% 收益在选择窗口 | （口径错误，作废） | — | ❌ 残差样本外零复现 | **拒绝（前视+证伪）** |
| DOS 波动预测 | ✅ | ❌ GBDT 零入树 | — | — | **拒绝（太弱，仅作极端风控开关）** |

### 主线因子年审（2026-07-17，`validate_mainline_factors.py` / `validate_timing_factors.py`）

**截面选股（study_007，2020-2025 月度复算，未做行业/市值中性化的简化复算）：**

| 因子 | 关卡1 全期IC/t | 关卡2 分半 | 关卡3 训练→测试 | 判定 |
|------|---------------|-----------|----------------|------|
| ivol 低波动（40%） | +0.111 / t=7.2，IC正率81% | ✅ 两半均 p<0.001 | ✅ +0.117→+0.106 均显著 | **通过（主力，方向单调）** |
| ret_1m 反转（35%） | +0.051 / t=3.3，IC正率61% | ✅ 两半同号各 p<0.1 | ✅ +0.042→+0.059 测试期更强 | **通过** |
| roe（15%） | +0.010 / t=0.3（n=32月） | ⚠️ 2023前无财务数据 | ⚠️ 无法完整验证 | **证据不足（覆盖14.6%）** |
| or_yoy / netprofit_yoy（各5%） | ≈0.01 不显著 | ⚠️ 同上 | ⚠️ 同上 | **证据不足** |
| 组合（复算） | +0.085 / t=3.2，IC正率75% | ✅ 两半均 p<0.05 | ✅ 与原报告 IC=0.089 吻合 | **通过** |

- 结论：主线选股 α 由 ivol + 反转驱动，**两因子全部通过关卡1-3**（与新闻因子形成鲜明对照：分半稳、测试期不衰减）。财务因子权重合计25%，受财务缓存覆盖限制（2023前无 PIT 数据）无法完整验证，建议补足数据后复审。
- 注意：因子有效 ≠ 组合赚钱——原研究 2023-2024 熊市绝对收益 -1.7%、成本年化 ~7-8%，组合层问题不改变因子层结论。

**指数择时（时间序列版检查，2011-2026）：**

| 信号 | 标的 | 结果 | 判定 |
|------|------|------|------|
| MA250 趋势闸 | 沪深300 | MA上方 fwd60 +2.04% vs 下方 +0.48%，t=4.12*** | **通过** |
| 强制入场（val_q≤15% 抄底） | 沪深300 | MA下方：低估 +3.10% vs 非低估 -2.26%，t=11.88*** | **通过（HS300上无价值陷阱）** |
| val_q 估值分位水平值 | 沪深300 | 方向正确（负相关）但不显著 | 弱，仅作仓位缩放器 |
| val_q / 趋势闸 / 强制入场 | 中证1000 | fwd20 ρ=+0.31（**方向相反**）；分半符号翻转；趋势闸 t=-1.34；强制入场 t=-0.21 | **不通过（中证1000上不稳定）** |

- 结论：择时规则的有效性**集中在沪深300**；在中证1000上同套信号方向不稳甚至反号——若"中证1000增强"的超额主要来自期货贴水（premium）而非择时信号，则不受影响，但**不建议把 val_q/MA250 择时套在中证1000上**。

---

*维护: 每次有新因子被拒或通过，把案例补进"案例回溯"节 —— 清单的价值随案例数增长。*
