# -*- coding: utf-8 -*-
"""按 review 意见修正 quant_conclusion 仓库文档（远端 clone 目录）"""
import io
import os

BASE = r"C:\Users\liuqi\AppData\Local\Temp\quant_conclusion_clone"


def read(p):
    with io.open(p, "r", encoding="utf-8") as f:
        return f.read()


def write(p, s):
    with io.open(p, "w", encoding="utf-8", newline="\n") as f:
        f.write(s)


# ============ 1. news_sentiment_prediction.md ============
p = os.path.join(BASE, "STOCK", "news_sentiment_prediction.md")
s = read(p)

s = s.replace(
    "> 状态: ✅ 已确认（T+1/T+2/T+3 前瞻收益验证）",
    "> 状态: ⚠️ 未验证（样本仅 78 个交易日，r≈0.03~0.06 效应量极弱，显著性不充分；仅作参考，不可直接使用）"
)
s = s.replace(
    "> 验证方式: 132,998 条财经新闻 + 3,078,880 条价格数据实证（2024-10 ~ 2025-02）",
    "> 验证方式: 132,998 条财经新闻 + 3,078,880 条价格数据实证（2024-10-11 ~ 2025-02-20，**78 个交易日**）\n"
    "> 注: 此前文档误写\"390 个交易日\"，实为 78 天 × 5 个预测窗口的行数；样本仅 78 个交易日，统计显著性不足"
)
s = s.replace(
    "- ✅ 新闻影响需要 1-2 天释放，**T+2 是最佳预测窗口**（相关系数最高 0.059），T+1 次之（0.047）。",
    "- ⚠️ 新闻影响需要 1-2 天释放，**T+2 是最佳预测窗口**（相关系数最高 0.059），T+1 次之（0.047）。\n"
    "- ⚠️ **效应量极弱**：r≈0.03~0.06，学术上属于\"有方向但极弱\"的相关；叠加样本仅 78 个交易日（2024-10-11~2025-02-20），"
    "相关系数未做显著性检验，**不足以支撑\"已确认\"结论**。"
)
s = s.replace(
    "- ✅ 负面情感的预测力约为正面 **1.5 倍**（印证 Tetlock 2007 的市场反应不足理论）。",
    "- ⚠️ 负面情感的预测力约为正面 **1.5 倍**（r：负面 -0.041~-0.046 vs 正面 +0.030~+0.034）。\n"
    "- ⚠️ **Tetlock (2007) 引用需修正**：该文核心是悲观新闻情绪导致**短期下行后随后反转**（情绪过度反应的均值回归），"
    "并非单纯的\"市场反应不足\"。本项目负相关方向一致，但\"反应不足\"的表述是对原文的简化，不宜作为理论背书。"
)
s = s.replace(
    "- ✅ 学术 Net 情感（事件分类 + 负面加权）多空策略 T+3 收益 **+0.925%**，Sharpe **0.419**（390 个交易日）。",
    "- ⚠️ 学术 Net 情感（事件分类 + 负面加权）多空策略 T+3 收益 **+0.925%**，日频 Sharpe **0.419**（**78 个交易日**）。\n"
    "- ⚠️ **显著性边界**：n=78 且 T+3 收益含重叠标签，有效独立样本更小。按年化口径估算 t≈0.5、按日频独立口径 t≈3.7，"
    "两种口径结论相反，说明统计量对假设高度敏感，**不能认定显著**。\n"
    "- ⚠️ 且多空腿包含做空，A 股做空成本高（融券/对冲），实际可执行性存疑（见避坑清单）。"
)
s = s.replace(
    "- ✅ LLM 精细情感（否定词反转 + 程度词 + 转折句降权）比简单情感提升 22%（r 0.038 → 0.047）。",
    "- ⚠️ LLM 精细情感（否定词反转 + 程度词 + 转折句降权）比简单情感提升 22%（r 0.038 → 0.047），提升幅度同样处于极弱相关区间。"
)
s = s.replace(
    "- ✅ 高 DOS → 高波动（T+1 波动 1.97% → 4.63%），用于波动率预测，不用于方向判断。",
    "- ⚠️ 高 DOS → 高波动（T+1 波动 1.97% → 4.63%），可用于波动率预测；但波动与方向无关，且样本仅 78 日，仅作参考。"
)
s = s.replace(
    "## 避坑清单\n\n- 公司名匹配精度有限（原始新闻中股票代码极少），匹配错误会污染样本。\n- 结论基于 2024-10 ~ 2025-02 区间，风格与新闻环境变化后需重新验证。",
    "## 避坑清单\n\n- ⚠️ **样本极小且统计不显著**：仅 78 个交易日、r≈0.03~0.06、多空 Sharpe 0.419（含做空成本后更低），"
    "本研究的价值是**方向提示**（负面情感、DOS、T+2 窗口），不是可交易的已确认信号。\n"
    "- 公司名匹配精度有限（原始新闻中股票代码极少），匹配错误会污染样本。\n- 结论基于 2024-10 ~ 2025-02 区间，风格与新闻环境变化后需重新验证。"
)
write(p, s)
print("[1] news_sentiment_prediction.md 已修正")

# ============ 2. style_neutralized_index_enhancement.md ============
p = os.path.join(BASE, "STOCK", "style_neutralized_index_enhancement.md")
s = read(p)

s = s.replace(
    "> 状态: ✅ 已确认",
    "> 状态: ✅ 已确认（⚠️ 注意：MaxDD 数字沿用旧口径，待按修正公式复核，见下）"
)
s = s.replace(
    "| 纯选股无风控 | 7.09% | 0.47 | -36.57% | 风控缺失，回撤大 |",
    "| 纯选股无风控 | 7.09% | 0.47 | -36.57% ⚠️ | 风控缺失，回撤大（MaxDD 待复核，见下注） |"
)
s = s.replace(
    "## 避坑清单",
    "## ⚠️ MaxDD 口径说明（review 修正）\n\n"
    "本文件第三节的 MaxDD 数字（-36.57% 等）取自 final_quant 仓库旧版回测，未标注计算公式。"
    "回撤控制研究（[risk_control.md](./risk_control.md)）已确认此前部分回测存在**计算 bug：把净值绝对差当百分比**"
    "（正确口径为 `(cummax-nav)/cummax` 的相对回撤），并据此将同框架 BASE+VAL 的 MaxDD 从 36.5% 修正为 19.5%。\n\n"
    "⚠️ 本文件策略配置（final_quant 微观因子、Top5~Top100）与 risk_control（factor_dic 价值因子、Top50）**不同**，"
    "MaxDD 数字不能直接替换为 19.5%；但旧口径 bug 的嫌疑相同，**这些 MaxDD 数字需回源 final_quant 用修正公式复核后才能采信**。\n\n"
    "## 避坑清单"
)
write(p, s)
print("[2] style_neutralized_index_enhancement.md 已修正")

# ============ 3. option_enhanced_study005.md ============
p = os.path.join(BASE, "STOCK", "option_enhanced_study005.md")
s = read(p)

s = s.replace(
    "> 状态: ✅ 已确认（仓库内审计；⚠️ 实盘部署前建议独立复核）",
    "> 状态: ⚠️ 待独立复核（仅为仓库自述审计；Sharpe 3.08/MaxDD -8.3% 异常亮眼，未独立验证前**不可直接使用**）"
)
s = s.replace(
    "> 验证方式: 严格 T+1 物理清算回测 + Worst-Case 盘中假设（仓库自述审计）",
    "> 验证方式: 严格 T+1 物理清算回测 + Worst-Case 盘中假设（**仅仓库自述审计，无独立复核**）"
)
s = s.replace(
    "## 已确认结论",
    "## 主要结论（待独立复核）"
)
s = s.replace(
    "- ✅ 剥离所有未来函数后，期权特征（QVIX Z-Score 隐含波动率 + PCR 情绪大闸）使全周期 **夏普比率 2.33 → 3.08**，最大回撤锁定 **-8.3%**。",
    "- ⚠️ 剥离所有未来函数后，期权特征（QVIX Z-Score 隐含波动率 + PCR 情绪大闸）声称使全周期 **夏普比率 2.33 → 3.08**、最大回撤锁定 **-8.3%**。\n"
    "- ⚠️ **警惕**：对 A 股 T+1 隔夜策略而言，Sharpe 3.08 / MaxDD 仅 -8.3% 属异常高水平；且验证方式仅为仓库自述审计、无独立复核、无 OOS 拆分。结论在独立复核前**按 ⚠️ 未验证对待**。"
)
write(p, s)
print("[3] option_enhanced_study005.md 已修正")

# ============ 4. risk_control.md（修正过度声称） ============
p = os.path.join(BASE, "STOCK", "risk_control.md")
s = read(p)

s = s.replace(
    "→ walk-forward 与固定 0.98 相当（甚至略优），**验证通过**：MA20 三档回撤控制在三段子区间均稳健、参数高原平坦、滚动 OOS 选参无损失。样本内调参风险排除。",
    "→ walk-forward 与固定 0.98 相当（甚至略优），**规则稳健性验证通过**：MA20 三档回撤控制在三段子区间均改善、参数高原平坦（0.95~1.00 全有效）、滚动 OOS 选参无损失。\n\n"
    "⚠️ **修正过度声称**：上述验证只能说明 **deep 阈值（0.98）** 的调参风险低（高原内任选均可），"
    "**不能排除 MA20 三档规则本身是在本样本期内被发现/确认的样本内偏差**——walk-forward 每年选出的 deep 与全样本最优几乎一致、"
    "结果与固定 0.99 完全相同，恰恰说明选参环节没有信息量，而非规则已被独立证明。\n"
    "若要真正排除规则层面的样本内偏差，需用 **2020 年之前或完全不同的市场/品种数据**做独立 OOS 检验。"
)
write(p, s)
print("[4] risk_control.md 已修正")

# ============ 5. 顶层 README.md（补索引） ============
p = os.path.join(BASE, "README.md")
s = read(p)

s = s.replace(
    "  - [新闻情感与股价预测（news_stock_research）](./STOCK/news_sentiment_prediction.md)",
    "  - [新闻情感与股价预测（news_stock_research）](./STOCK/news_sentiment_prediction.md)（⚠️ 未验证）\n"
    "  - [回撤控制研究（risk_control.md）](./STOCK/risk_control.md)"
)
write(p, s)
print("[5] README.md 已修正")

print("全部完成")
